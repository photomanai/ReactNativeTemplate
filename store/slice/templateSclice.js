import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const templateSclice = createSlice({
  name: "template",
  initialState,
  reducers: {},
});

export const {} = templateSclice.actions;

export default templateSclice.reducer;
